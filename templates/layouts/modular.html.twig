{% set grid_size = theme_var('grid-size') %}

<section class="section {{ template_name }} {{ page.header.class }}">
    <div class="container">
        {% block modular_content %}{% endblock %}
    </div>
</section>
